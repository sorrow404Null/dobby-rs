# dobby-hook

Facade crate for `dobby-hook-core` with an optional `framework` feature.

Most users should depend on this crate:

```toml
[dependencies]
dobby-hook = { version = "0.1.1", features = ["framework"] }
```

Project overview and examples:

- Repository README: `../README.md`
